/**
 * Max decimal place for any digit to compute
 */
export const MAX_COMPUTE_DECIMAL_PLACE = 2;
/**
 * Max delay users can set on computation
 */
export const MAX_COMPUTE_DELAY_SEC = 604800; // 1week
/**
 * Min delay users can set on computation
 */
export const MIN_COMPUTE_DELAY_SEC = 0;
export const DEFAULT_LISTENNING_PORT_IF_NON_SPECIFIED = 3000;
/**
 * Queue where delaye jobs would be
 */
export const DELAY_QUEUE = "delay-queue";
export const DEFAULT_QUERY_LIMIT = 10;
